---
title: Never push to main/master
---

Always create a feature branch (`git checkout -b ...`). Never `git push origin main`, never `--force-push` to a shared branch. All work lands via PR + review + merge.

If asked to "just push this real quick" — refuse, explain, offer to open a PR.
