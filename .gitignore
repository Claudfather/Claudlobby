# Secrets — never commit
*.env
.env
.env.*
!.env.seed.example
!.env.example

# Generated MCP configs (composed from library/mcp/ fragments at generate time)
.mcp.json

# OAuth credentials
.google_workspace_mcp/

# Compositor output — gitignored. Bots may edit themselves here freely;
# promote useful changes back to library/ via `claudlobby promote`.
runtime/

# Local fleet overlays — user's fleet-specific library, voices, fleet.yaml,
# runtime, .env, etc. Never committed to the public claudlobby repo.
local/

# Local-only voices (legacy path — use local/<fleet>/voices/ now)
voices/local/

# Claude Code state (auth, channels, sessions) — but allow project-level skills
.claude/*
!.claude/skills/

# Python build artefacts
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
build/
dist/

# Node modules
node_modules/

# Cloned project repos (legacy: bots clone repos under their own dir)
*/projects/

# Logs
# Runtime state (fleet-state.json, creds-check-state.json, locks)
state/
lib/fleet-state.json
lib/fleet-state.json.lock
lib/creds-check-state.json
lib/creds-check-state.json.lock
lib/host-health-check.state

*.log

# OS
.DS_Store

# Skill config files (real tokens — only config.example.json is committed)
library/skills/*/config.json

# Claudron vault pointer (machine-specific)
.claudron

# Active fleet config (root mode) — the .example files are the canonical templates
fleet.yaml
projects.yaml

# Claude fleet bot telemetry — supervision hooks may write these relative to
# the session cwd instead of the bot directory (Claudfather/Claudlobby#874);
# they can land at any depth and are never product data.
**/data/events/fleet-*.jsonl
**/data/.last-tool-call
**/data/.idle
